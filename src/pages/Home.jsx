import React from 'react';
import Hero from '../components/sections/Hero';

const Home = ({ lang }) => {
    return (
        <>
            <Hero lang={lang} />
        </>
    );
};

export default Home;
